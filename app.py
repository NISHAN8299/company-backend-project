def login(username,possword):
   return f"User {username} logged in successfully with password {password}"

def login(username,password):
   if username == "admin":
      return "welcome admin"
   return f"User {username} logged in successfully"